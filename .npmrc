# Next.js / Turbopack resolves some transitive deps (postcss, scheduler) that
# aren't direct dependencies of apps/web. A hoisted layout keeps that working
# while staying on pnpm.
node-linker=hoisted
